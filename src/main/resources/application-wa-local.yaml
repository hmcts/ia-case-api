logging.level:
  root: info
  org.springframework.web: debug
  uk.gov.hmcts: debug

ref-data-case-worker-api:
  url: ${REF_DATA_CASE_WORKER_URL:http://wiremock}


idam:
  ia_system_user:
    scope: "openid profile authorities"

launchDarkly:
  sdkKey: ${IA_LAUNCH_DARKLY_SDK_KEY:sdk-key}
